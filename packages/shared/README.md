# Shared Package

Shared TypeScript types and constants used by both the web and API apps.

This package will hold cross-cutting definitions (e.g. locker sizes, package statuses, pickup codes) so the frontend and backend stay aligned.
