# API

Fastify + TypeScript backend (planned).

This service will handle:

- Agent validation
- Locker availability
- Package drop-off
- Pickup code validation
- Package retrieval
- MongoDB integration
